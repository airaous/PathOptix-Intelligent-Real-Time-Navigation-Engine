[build]
# Zeabur will auto-detect Python and use the Dockerfile

[deploy]
# Environment variables
PORT = "8000"

[runtime]
# Python runtime configuration
python_version = "3.9"

[health_check]
# Health check configuration
path = "/health"
interval = 30
timeout = 10
retries = 3
